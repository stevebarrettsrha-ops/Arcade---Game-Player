==============================================================
  ARCADE — STREAMING MODE (run real emulators on the host)
  Works on Windows, macOS and Linux hosts
==============================================================

WHAT IT DOES
  Normally every TV/phone runs the emulator itself (in the browser). In
  streaming mode the HOST runs the REAL native emulator with full power (GPU +
  JIT) and ARCADE streams the picture into the page on the clients. That gets
  you the heavy / "impossible" systems a browser can't do — PS2 (PCSX2),
  original XBOX (xemu), GameCube/Wii (Dolphin), PS3 (RPCS3), and full-speed
  N64/PSP — while weak TVs only have to show a video. Your phone (pad.html)
  is the controller: its buttons are injected into the emulator on the host.

  This is OPT-IN. With it off, ARCADE behaves exactly as before.

------------------------------------------------------------
1. INSTALL ffmpeg ON THE HOST  (one time)
------------------------------------------------------------
  ffmpeg captures the screen and encodes the video.

  Windows :  winget install ffmpeg        (or download from ffmpeg.org)
  macOS   :  brew install ffmpeg
  Linux   :  sudo apt install ffmpeg xdotool
  Steam Deck (Desktop Mode, Konsole):
     sudo steamos-readonly disable ; sudo pacman-key --init
     sudo pacman -S ffmpeg xdotool

  Controller input needs ONE more thing per OS:
    Windows : nothing required — ARCADE injects keys itself (stream-input.ps1).
              BEST EXPERIENCE (recommended): install the ViGEmBus driver and
              drop ViGEmClient.dll next to server.js. ARCADE then drives a
              REAL virtual Xbox controller (stream-gamepad.ps1) instead of
              faking keystrokes, so the analog stick + every button work in
              EVERY emulator with NO per-emulator key mapping. See section 4a.
    macOS   : nothing to install, but grant the Terminal/Node both
              "Screen Recording" and "Accessibility" permission when asked
              (System Settings > Privacy & Security).
    Linux   : xdotool (installed above). X11 desktops only — on Wayland,
              run the emulator from an X11 session.

------------------------------------------------------------
2. ADD YOUR EMULATORS
------------------------------------------------------------
  WINDOWS — mostly automatic. Six emulators come PRE-CONFIGURED in
  emulators/:  Project64 (N64), PCSX2 (PS2), PPSSPP (PSP), xemu (XBOX),
  FreeJ2ME and KEmulator nnmod (Java). Double-click

      download-emulators-windows.bat        (one time, online)

  It downloads the portable ones (xemu, KEmulator, FreeJ2ME) from their
  official releases into emulators/<name>/app/ and checks the rest.
  PCSX2, PPSSPP and Project64 use normal Windows installers, so run the
  installer you downloaded yourself — the manifests already point at their
  default install paths (edit emulators/<name>/emulator.json if you chose
  a different folder). Each folder has a short _SETUP.txt with specifics.
  start-windows-stream.bat also offers this setup on first run.

  LINUX / macOS (or other emulators): manifests for Dolphin, RPCS3,
  RetroArch-N64 and Linux variants are in  emulators/_examples/ .
  Copy a folder one level up (e.g. emulators/_examples/dolphin ->
  emulators/dolphin), then edit its "cmd" line to match how the emulator
  is launched on YOUR computer. See emulators/_examples/README.txt.

  Manifest format (emulators/<name>/emulator.json):
     {
       "name":    "PCSX2 (PS2)",
       "cmd":     "\"C:\\Program Files\\PCSX2\\pcsx2-qt.exe\" -fullscreen {rom}",
       "capture": "auto",
       "window":  "PCSX2",
       "needsRom": true,
       "system":  "PS2",
       "roms":    "games/ps2",
       "exts":    [".iso", ".chd"]
     }

  Fields: name (shown in ARCADE) · cmd ({rom} = the game file; quote paths
  with spaces) · capture (auto | x11 | gdigrab | avfoundation | kms | test)
  · window (title, for input focus on X11) · needsRom · system (label on the
  cards) · roms (a folder inside ARCADE whose files are listed as launchable
  games) · exts (which files in that folder count). Optional icon.png in the
  folder is used as the cover.

  With "roms" set, the games in that folder show up BOTH in ARCADE's
  "Host emulators" picker AND in the phone pad's Games drawer — so a phone
  can start a PS2/XBOX game on the TV directly.

------------------------------------------------------------
3. START ARCADE WITH STREAMING ON
------------------------------------------------------------
  Double-click the matching script:
     Windows :  start-windows-stream.bat
     macOS   :  start-mac-stream.command
     Linux   :  ./start-linux-stream.sh
  (Equivalent to:  ARCADE_STREAM=1 node server.js)

  Tuning (env vars, all optional):
     ARCADE_STREAM_MODE=auto       # auto | mjpeg | h264  (see "Picture quality")
     ARCADE_STREAM_CAPTURE=auto    # auto picks per-OS: Windows gdigrab,
                                   # macOS avfoundation, Linux x11 · or: test
     ARCADE_STREAM_DISPLAY=:0      # X display (Linux) / screen device (macOS)
     ARCADE_STREAM_SIZE=1280x720   # stream size (desktop grabs are scaled)
     ARCADE_STREAM_FPS=30
     ARCADE_STREAM_QUALITY=6       # MJPEG quality 2(best)..31 (smaller/faster)
     ARCADE_STREAM_VBITRATE=6M     # H.264 target bitrate (h264/auto mode)
     ARCADE_STREAM_INPUT=auto      # auto | gamepad | xdotool | powershell | osascript | none
                                   # auto = on Windows, use "gamepad" (virtual
                                   # Xbox pad) if ViGEmClient.dll is present,
                                   # else fall back to keyboard injection.
     ARCADE_STREAM_FFMPEG=ffmpeg   # path/override, e.g. for a Flatpak ffmpeg
     ARCADE_STREAM_AUDIO=auto       # auto | none | pulse|alsa|dshow|avfoundation
     ARCADE_STREAM_AUDIO_DEVICE=    # the sound source to capture (see below)
     ARCADE_STREAM_AUDIO_RATE=48000

  PICTURE QUALITY — two transports, chosen per client automatically:
     • H.264: the host encodes the screen + sound into ONE fragmented-MP4
       stream (video and audio share a timeline, so they stay in sync) and the
       client plays it in a <video> via Media Source Extensions. Sharper,
       smoother and uses far less WiFi than MJPEG. Needs libx264 in ffmpeg
       (almost all builds have it) and a reasonably modern browser.
     • MJPEG + PCM: the original fallback — a stream of JPEG frames plus raw
       audio — for old TV browsers that don't support MSE.
     ARCADE_STREAM_MODE=auto (default) lets each device pick the best it can
     play. Force one with =h264 or =mjpeg. In auto/h264 mode you'll see the
     same "🔊 Tap for sound" button (it unmutes the muxed video).

  WANT TO TEST THE PIPELINE WITH NO EMULATOR?  Start with
     ARCADE_STREAM_CAPTURE=test — open ARCADE, click "🖥️ Host emulators",
  pick anything: you should see a moving test pattern. That proves ffmpeg +
  the stream path work; then switch back to auto.

------------------------------------------------------------
3b. SOUND (the game's audio, streamed to every device)
------------------------------------------------------------
  ARCADE streams the host's audio next to the video. On the client, the
  stream shows a "🔊 Tap for sound" button (browsers require one tap before
  any audio can start) — tap it and you hear the game; tap again to mute.

  You must tell ffmpeg WHICH sound source to capture — the speakers/game
  output, not a microphone. Set ARCADE_STREAM_AUDIO_DEVICE per OS:

    Linux (PulseAudio/PipeWire):
        list sources:   pactl list sources short
        pick the one ending in ".monitor" (that's the speaker output), e.g.
        ARCADE_STREAM_AUDIO_DEVICE="alsa_output.pci-0000_00_1f.3.analog-stereo.monitor"

    Windows (DirectShow):
        enable "Stereo Mix" in  Settings > System > Sound > More sound
        settings > Recording  (right-click > Show Disabled Devices). The
        default already targets  audio=Stereo Mix . If your PC has no Stereo
        Mix, install a virtual cable (e.g. VB-CABLE) and set:
        set ARCADE_STREAM_AUDIO_DEVICE=audio=CABLE Output (VB-Audio Virtual Cable)

    macOS (avfoundation):
        macOS can't capture system audio on its own — install a loopback
        device (e.g. BlackHole), set it as the output (or via a Multi-Output
        device so you still hear it), then:
        ARCADE_STREAM_AUDIO_DEVICE=":BlackHole 2ch"

  To turn sound off entirely:  ARCADE_STREAM_AUDIO=none
  Audio is independent of video — a wrong/missing device just means silence;
  the picture keeps streaming. Check  /stream/status  ("audio" / "audioError")
  if you're not hearing anything.

  PRIVACY: "auto" captures the OS default source, which on some machines is a
  microphone. Point ARCADE_STREAM_AUDIO_DEVICE at the speaker monitor/loopback
  above (or set ARCADE_STREAM_AUDIO=none) so you only stream game sound.

------------------------------------------------------------
4. PLAY
------------------------------------------------------------
  - On the TV: open ARCADE, click "🖥️ Host emulators", pick an emulator,
    then a game. It launches on the host and the picture appears in the page.
  - On a phone: tap "📱 Phone", scan the code — the phone is now the
    controller, and its Games drawer also lists the host games (PS2, XBOX,
    ...) so you can start them from the sofa.
  - VIRTUAL XBOX PAD (Windows, recommended — see 4a): if ViGEmBus +
    ViGEmClient.dll are installed, ARCADE presents a real Xbox controller to
    the host. Emulators detect it automatically — analog stick, triggers and
    every button just work, with NO key mapping to do. This is the default on
    Windows when the DLL is present.
  - KEYBOARD FALLBACK (other setups): buttons are sent as keyboard presses
    (RetroArch's default layout: arrows = D-pad, Z/X = B/A, A/S = Y/X,
    Q/W = L/R, E/T = L2/R2, Enter = Start, Shift = Select; SAVE=F2, LOAD=F4,
    FF=Space). The LEFT ANALOG STICK sends its OWN keys — U/N/H/M
    (up/down/left/right) — so bind the emulator's analog stick to U N H M and
    the D-pad to the arrows. Map those keys inside the emulator once (every
    emulator lets you bind keyboard keys to its pads) and the phone drives it.
    (The stick also still presses the D-pad arrows, so it works even before you
    bind U/N/H/M; the separate keys just let it move the real analog nub too.
    It's 8-way digital on this path — for smooth analog use the virtual pad.)
      macOS host note: AppleScript can't hold arrow keys, so the D-pad sends
      I/K/J/L there — bind those instead (the analog stick is still U/N/H/M).

------------------------------------------------------------
4a. VIRTUAL XBOX CONTROLLER (Windows) — analog + all buttons, zero mapping
------------------------------------------------------------
  Faking keystrokes means every emulator has to be re-bound to ARCADE's
  keyboard layout, and the analog stick can only ever be 8-way digital.
  A virtual controller fixes both at once. ONE-TIME setup on the host:

    1. Install the ViGEmBus driver (free, open source):
         https://github.com/nefarius/ViGEmBus/releases
       Run the installer (ViGEmBus_Setup_x64.msi) and reboot if asked.

    2. Get ViGEmClient.dll (the x64 build) and put it NEXT TO server.js
       (the ARCADE folder). You can also point ARCADE at it elsewhere with
         set ARCADE_VIGEM_DLL=C:\path\to\ViGEmClient.dll

    3. Start ARCADE with start-windows-stream.bat as usual. On launch it
       auto-detects the DLL and switches input to "gamepad" — you'll see a
       virtual Xbox 360 pad appear in Windows "Game Controllers".

  Now the phone pad drives a real controller: the analog stick is true
  analog, L2/R2 are the triggers, and every button maps to its Xbox
  equivalent — so you don't bind anything inside PPSSPP/PCSX2/xemu/etc.
  Force it on/off with ARCADE_STREAM_INPUT=gamepad / =powershell.

------------------------------------------------------------
LIMITS / NOTES
------------------------------------------------------------
  - On modern browsers ARCADE streams H.264 + AAC muxed into one container,
    so audio and video are in sync (true lip-sync) and bandwidth is low.
    Old TV browsers without Media Source Extensions fall back to MJPEG video
    + separate PCM audio; those two aren't frame-locked, so expect a small
    lip-sync offset on that path (fine for games), and tap "Tap for sound"
    to start audio where Web Audio is supported.
  - H.264 needs an ffmpeg built with libx264 (the vast majority are). If it's
    missing, the encoder won't start and clients drop back to MJPEG; check
    /stream/status ("muxError") if H.264 never appears.
  - The phone's analog stick is TRUE analog on the Windows virtual-gamepad
    path (ViGEmBus, section 4a). On the keyboard fallback paths it arrives as
    8-way digital presses on its OWN keys (U/N/H/M) — separate from the D-pad
    arrows, so you can bind the emulator's analog stick to it. A Linux/macOS
    virtual pad (uinput / a HID shim) for smooth analog is still planned.
  - One host = one stream at a time (everyone watches the SAME game).
  - Streaming your WHOLE DESKTOP instead of just the game? Two fixes:
      * Fullscreen emulators (PCSX2/PPSSPP/xemu): they pass a -fullscreen
        flag, so with capture:"auto" the screen IS the game. Make sure the
        emulator actually goes fullscreen.
      * Windowed emulators (Project64, the Java handsets): set
        capture:"window" in their emulator.json (already done for the
        pre-wired ones). ARCADE then streams ONLY that emulator window — by
        its "window" title — instead of the desktop, which also stops the
        recursive "browser showing the stream" mirror effect. Windows only;
        on macOS/Linux it falls back to a full-screen grab.
        If the game area comes out BLACK with capture:"window", the
        emulator's video backend can't be window-grabbed — run it fullscreen
        with capture:"auto", or switch its video plugin/backend.
  - You supply your own legally-owned emulators, BIOS files and games.
    Nothing is bundled or downloaded by ARCADE.
==============================================================
